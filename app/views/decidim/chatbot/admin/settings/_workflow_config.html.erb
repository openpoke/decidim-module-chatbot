<% if workflow_manifest&.configurable? %>
  <div class="card">
    <div class="card-divider">
      <h2 class="card-title">
        <%= t("decidim.chatbot.admin.settings.form.configuration_for", workflow: workflow_manifest.title) %>
      </h2>
    </div>

    <div class="card-section">
      <%= render partial: workflow_manifest.settings_partial, locals: { form: form } %>
    </div>
  </div>
<% end %>
